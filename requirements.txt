"""
Arquivo de dependências do projeto (requirements.txt)

Para instalar todas as dependências necessárias, execute:
    pip install -r requirements.txt

Dependências principais:
- Flask: Framework web para criar a API
- pytest: Framework para testes
- pytest-cov: Plugin para cobertura de código
- pytest-flask: Plugin pytest para Flask
- pytest-mock: Plugin para mocking
"""

Flask==3.0.0
pytest==7.4.3
pytest-cov==4.1.0
pytest-flask==1.3.0
pytest-mock==3.12.0
requests==2.31.0
python-dotenv==1.0.0
